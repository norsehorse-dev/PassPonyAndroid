package com.passpony.android.ui.theme

import androidx.compose.material3.Typography

// The M3 default type scale is fine for v1; entry names and passwords use
// explicit monospaced styles at their call sites (P07) rather than here.
val PassPonyTypography = Typography()
